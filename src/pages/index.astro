---
// Component Imports
import Counter from '../components/Counter';
import Layout from '../components/Layout';
import Poke from '../components/Poke.astro';
const someProps = {
	count: 0,
};

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/

const pokemons = await fetch(`https://pokeapi.co/api/v2/pokemon?limit=10`).then(res => res.json())
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<style>
			html,
			body {
				font-family: system-ui;
				margin: 0;
			}
			body {
				padding: 2rem;
			}
		</style>
	</head>
	<body>
		<main>
		<h1>hola</h1>
			<!-- <Layout client:load/> -->
			<!-- {JSON.stringify(pokemons.results, null, 2)} -->
			{pokemons.results.map(poke => <Poke name={poke.name} />)}
		</main>
	</body>
</html>
