---
const {name} = Astro.props
const poke = await fetch(`https://pokeapi.co/api/v2/pokemon/${name}`).then(res => res.json())
---

<li>
<span>{poke.name}</span>
<!-- <Image 
  src={poke}
  width="96"
  aspecRation={1}
  format="webp"
  alt={poke.name}
/> -->
</li>